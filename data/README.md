# Data and model assets

No datasets, relevance judgements, course-supplied samples, retrieval indexes, model weights, cached results, or generated answers are distributed in this repository.

To run the notebooks, obtain each dependency and dataset through its official source under the relevant licence. Keep downloaded assets outside version control.
